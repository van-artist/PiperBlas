#include <cuda_runtime.h>
#include "pi_type.h"

__global__ void gemm_fp32_kernel(int m, int k, int n, float *A, float *B, float *C, float alpha, float beta)
{
}

piState gemm_fp32(int m, int k, int n, float *A, float *B, float *C, float alpha, float beta)
{
}
